<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/patientCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/initialLetter"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:textSize="@dimen/patient_initial_size"
        android:padding="10dp"
        android:visibility="invisible"
        android:textColor="@color/colorPrimaryDark"
        android:text="A"/>

    <ImageView
        android:id="@+id/patientIcon"
        android:layout_toRightOf="@+id/initialLetter"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:clickable="true"
        android:scaleType="fitXY"
        android:scaleX="0.8"
        android:scaleY="0.8"
        android:src="@drawable/male" />

    <TextView
        android:id="@+id/patientName"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:padding="10dp"
        android:layout_toRightOf="@+id/patientIcon"
        android:text="Patient name"
        android:textColor="@color/album_title"
        android:textSize="@dimen/patient_name_size"
        android:transitionName="@string/fragment_text_trans" />


    <!--<ImageView-->
        <!--android:id="@+id/overflow"-->
        <!--android:layout_width="@dimen/ic_album_overflow_width"-->
        <!--android:layout_height="@dimen/ic_album_overflow_height"-->
        <!--android:layout_alignParentRight="true"-->
        <!--android:layout_gravity="right"-->
        <!--android:layout_marginTop="@dimen/ic_album_overflow_margin_top"-->
        <!--android:scaleType="centerCrop"-->
        <!--android:src="@drawable/ic_dots" />-->


</RelativeLayout>